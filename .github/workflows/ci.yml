name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9.12.0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: pulseops/pnpm-lock.yaml
      - run: pnpm install --no-frozen-lockfile
        working-directory: pulseops
      - run: pnpm db:generate
        working-directory: pulseops
      - run: pnpm lint
        working-directory: pulseops
      - run: pnpm typecheck
        working-directory: pulseops
      - run: pnpm build
        working-directory: pulseops
